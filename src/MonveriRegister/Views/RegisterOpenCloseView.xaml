<UserControl x:Class="MonveriRegister.Views.RegisterOpenCloseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:MonveriRegister.ViewModels"
             d:DataContext="{d:DesignInstance vm:RegisterOpenCloseViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{StaticResource BackgroundBrush}">

    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="{Binding Title}" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
            <TextBlock FontSize="14" Foreground="{StaticResource TextSecondaryBrush}">
                <Run Text="Employee:"/>
                <Run Text="{Binding EmployeeName, Mode=OneWay}"/>
            </TextBlock>
        </StackPanel>

        <!-- Denomination counting grid -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel MaxWidth="800">
                <!-- Tax location selection (opening only) -->
                <Border Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="20" Margin="0,0,0,16"
                        Visibility="{Binding IsOpening, Converter={StaticResource BoolToVis}}">
                    <StackPanel>
                        <TextBlock Text="Tax Location" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
                        <ComboBox ItemsSource="{Binding TaxLocations}" SelectedItem="{Binding SelectedTaxLocation}"
                                  DisplayMemberPath="LocationName" FontSize="14" Padding="8,6"/>
                        <CheckBox Content="Training Mode" IsChecked="{Binding IsTraining}" Margin="0,12,0,0" FontSize="13"/>
                    </StackPanel>
                </Border>

                <!-- Bills -->
                <Border Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="20" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Bills" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12" Foreground="{StaticResource SuccessBrush}"/>
                        <UniformGrid Columns="3" Rows="2">
                            <StackPanel Margin="4">
                                <TextBlock Text="$100" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBox Text="{Binding Bills100, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource RegisterTextBox}" FontSize="18"/>
                            </StackPanel>
                            <StackPanel Margin="4">
                                <TextBlock Text="$50" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBox Text="{Binding Bills50, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource RegisterTextBox}" FontSize="18"/>
                            </StackPanel>
                            <StackPanel Margin="4">
                                <TextBlock Text="$20" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBox Text="{Binding Bills20, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource RegisterTextBox}" FontSize="18"/>
                            </StackPanel>
                            <StackPanel Margin="4">
                                <TextBlock Text="$10" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBox Text="{Binding Bills10, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource RegisterTextBox}" FontSize="18"/>
                            </StackPanel>
                            <StackPanel Margin="4">
                                <TextBlock Text="$5" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBox Text="{Binding Bills5, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource RegisterTextBox}" FontSize="18"/>
                            </StackPanel>
                            <StackPanel Margin="4">
                                <TextBlock Text="$1" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBox Text="{Binding Bills1, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource RegisterTextBox}" FontSize="18"/>
                            </StackPanel>
                        </UniformGrid>
                    </StackPanel>
                </Border>

                <!-- Coins -->
                <Border Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="20" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Coins" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12" Foreground="{StaticResource InfoBrush}"/>
                        <UniformGrid Columns="3" Rows="2">
                            <StackPanel Margin="4">
                                <TextBlock Text="Dollar" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBox Text="{Binding CoinsDollar, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource RegisterTextBox}" FontSize="18"/>
                            </StackPanel>
                            <StackPanel Margin="4">
                                <TextBlock Text="Half Dollar" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBox Text="{Binding CoinsHalfDollar, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource RegisterTextBox}" FontSize="18"/>
                            </StackPanel>
                            <StackPanel Margin="4">
                                <TextBlock Text="Quarter" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBox Text="{Binding CoinsQuarter, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource RegisterTextBox}" FontSize="18"/>
                            </StackPanel>
                            <StackPanel Margin="4">
                                <TextBlock Text="Dime" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBox Text="{Binding CoinsDime, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource RegisterTextBox}" FontSize="18"/>
                            </StackPanel>
                            <StackPanel Margin="4">
                                <TextBlock Text="Nickel" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBox Text="{Binding CoinsNickel, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource RegisterTextBox}" FontSize="18"/>
                            </StackPanel>
                            <StackPanel Margin="4">
                                <TextBlock Text="Penny" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBox Text="{Binding CoinsPenny, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource RegisterTextBox}" FontSize="18"/>
                            </StackPanel>
                        </UniformGrid>
                    </StackPanel>
                </Border>

                <!-- Rolls -->
                <Border Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="20" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Rolls" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12" Foreground="{StaticResource WarningBrush}"/>
                        <UniformGrid Columns="3" Rows="2">
                            <StackPanel Margin="4">
                                <TextBlock Text="Dollar ($25)" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBox Text="{Binding RollsDollar, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource RegisterTextBox}" FontSize="18"/>
                            </StackPanel>
                            <StackPanel Margin="4">
                                <TextBlock Text="Half Dollar ($10)" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBox Text="{Binding RollsHalfDollar, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource RegisterTextBox}" FontSize="18"/>
                            </StackPanel>
                            <StackPanel Margin="4">
                                <TextBlock Text="Quarter ($10)" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBox Text="{Binding RollsQuarter, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource RegisterTextBox}" FontSize="18"/>
                            </StackPanel>
                            <StackPanel Margin="4">
                                <TextBlock Text="Dime ($5)" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBox Text="{Binding RollsDime, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource RegisterTextBox}" FontSize="18"/>
                            </StackPanel>
                            <StackPanel Margin="4">
                                <TextBlock Text="Nickel ($2)" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBox Text="{Binding RollsNickel, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource RegisterTextBox}" FontSize="18"/>
                            </StackPanel>
                            <StackPanel Margin="4">
                                <TextBlock Text="Penny ($0.50)" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBox Text="{Binding RollsPenny, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource RegisterTextBox}" FontSize="18"/>
                            </StackPanel>
                        </UniformGrid>
                    </StackPanel>
                </Border>

                <!-- Straps -->
                <Border Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="20" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Straps" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12" Foreground="{StaticResource DangerBrush}"/>
                        <UniformGrid Columns="2" Rows="2">
                            <StackPanel Margin="4">
                                <TextBlock Text="$20 Strap ($500)" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBox Text="{Binding Straps20, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource RegisterTextBox}" FontSize="18"/>
                            </StackPanel>
                            <StackPanel Margin="4">
                                <TextBlock Text="$10 Strap ($250)" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBox Text="{Binding Straps10, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource RegisterTextBox}" FontSize="18"/>
                            </StackPanel>
                            <StackPanel Margin="4">
                                <TextBlock Text="$5 Strap ($100)" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBox Text="{Binding Straps5, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource RegisterTextBox}" FontSize="18"/>
                            </StackPanel>
                            <StackPanel Margin="4">
                                <TextBlock Text="$1 Strap ($25)" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBox Text="{Binding Straps1, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource RegisterTextBox}" FontSize="18"/>
                            </StackPanel>
                        </UniformGrid>
                    </StackPanel>
                </Border>

                <!-- Notes (closing only) -->
                <Border Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="20" Margin="0,0,0,12"
                        Visibility="{Binding IsOpening, Converter={StaticResource BoolToVis}, ConverterParameter=invert}">
                    <StackPanel>
                        <TextBlock Text="Notes" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
                        <TextBox Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource RegisterTextBox}"
                                 AcceptsReturn="True" MinHeight="60" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer: Total + Actions -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="20" Margin="0,12,0,0">
            <DockPanel>
                <StackPanel DockPanel.Dock="Left" VerticalAlignment="Center">
                    <TextBlock Text="Cash Total" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock Text="{Binding CalculatedTotal, StringFormat=C2}" FontSize="32" FontWeight="Bold"
                               Foreground="{StaticResource SuccessBrush}"/>
                </StackPanel>

                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center"
                            HorizontalAlignment="Right">
                    <Button Content="Cancel" Command="{Binding CancelCommand}"
                            Style="{StaticResource SecondaryButton}" Margin="0,0,8,0"/>

                    <!-- Open button -->
                    <Button Content="Open Register" Command="{Binding OpenRegisterCommand}"
                            Style="{StaticResource SuccessButton}" FontSize="16" Padding="24,12"
                            Visibility="{Binding IsOpening, Converter={StaticResource BoolToVis}}"
                            IsEnabled="{Binding IsProcessing, Converter={StaticResource InverseBool}}"/>

                    <!-- Close button -->
                    <Button Content="Close Register" Command="{Binding CloseRegisterCommand}"
                            Style="{StaticResource DangerButton}" FontSize="16" Padding="24,12"
                            Visibility="{Binding IsOpening, Converter={StaticResource BoolToVis}, ConverterParameter=invert}"
                            IsEnabled="{Binding IsProcessing, Converter={StaticResource InverseBool}}"/>
                </StackPanel>

                <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" HorizontalAlignment="Center"
                           Foreground="{StaticResource TextSecondaryBrush}" FontSize="13"/>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
